## 0.0.3

* Add `addressComponents` to `PlaceDetails` (Google Places `long_name` / `short_name` / `types` shape).

## 0.0.2

* Lower Dart SDK constraint to `^3.10.0` for broader Flutter/Dart compatibility.

## 0.0.1

* Initial release: Google Places SDK autocomplete and place details for Android and iOS.
